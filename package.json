{
  "name": "newapi-gatekeeper",
  "version": "1.0.0",
  "description": "Block JanitorAI traffic and forward clean requests to New API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
